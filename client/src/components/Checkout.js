import React from 'react'

function Checkout(){

    return(
        <div id='checkout'>
            <h1>Total</h1>
            <button>Continue to checkout</button>
        </div>
    )
}

export default Checkout;